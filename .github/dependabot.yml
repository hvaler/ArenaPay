version: 2
updates:
  # Las acciones van fijadas por SHA; Dependabot propone el SHA nuevo y actualiza el comentario de versión.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
